<div class="title"><h1 class="header">animate</h1></div>
<div class="field">
    <form method="POST">
        <input type="email" name="email" placeholder="email" required />
        <input
            type="password"
            name="password"
            placeholder="password"
            required
        />

        <div class="buttons">
            <button type="submit">log in</button>

    </form>
    {{#if exists}}<div id="exists"><p>
                ops! wrong username or password</div>{{/if}}
</div>
